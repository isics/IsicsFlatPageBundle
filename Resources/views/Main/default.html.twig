<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
    <head>
        <title>{{ page.title }}</title>
        <meta http-equiv="Content-Type" content="text/html;charset=UTF-8" />
        <meta type="description" content="{{ page.metaDescription }}" />
        <meta type="keywords" content="{{ page.metaKeywords }}" />
    </head>
    <body>
        <h1>{{ page.title }}</h1>
        {{ page.content|raw }}
    </body>
</html5>
